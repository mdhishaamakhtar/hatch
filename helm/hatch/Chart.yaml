apiVersion: v2
name: hatch
description: Hatch — future email scheduler. Deploys the data infrastructure (Postgres, Redis, Kafka KRaft) and all six services, each behind its own enabled toggle.
type: application
version: 0.1.0
appVersion: "0.1.0"

# Default values for the Hatch chart. Each Hatch service has its own `enabled`
# toggle so individual phases can opt in/out independently.

api:
  enabled: true
  image: hatch/api:dev
  imagePullPolicy: Always
  replicas: 1
  servicePort: 9021
  serviceType: LoadBalancer

scheduler:
  enabled: true
  image: hatch/scheduler:dev
  imagePullPolicy: Always
  replicas: 2
  adminPort: 9022
  scheduleChannelBuffer: 100000
  clearChannelBuffer: 64
  wheelStorage:
    size: 1Gi

postgres:
  image: postgres:17-alpine
  database: hatch
  user: hatch
  storage: 5Gi
  servicePort: 5432

redis:
  image: redis:7-alpine
  storage: 1Gi
  servicePort: 6379

kafka:
  image: apache/kafka:3.9.0
  storage: 5Gi
  brokerPort: 9092
  controllerPort: 9093
  clusterID: 5L6g3nShT-eMCtK--X86sw
  # Topics provisioned by the post-install/post-upgrade Job. Phase 4 extends
  # this list with the retry tiers (emails.retry.1min/5min/30min).
  topics:
    - name: emails.due
      partitions: 12
      replicationFactor: 1
